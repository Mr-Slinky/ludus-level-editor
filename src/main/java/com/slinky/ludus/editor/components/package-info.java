/**
 * The drawing surfaces and the controls that a user works the editor with.
 * <p>
 * {@link com.slinky.ludus.editor.components.LevelCanvas} is the grid that a user stamps tiles onto, and
 * {@link Swatch} is a single image cut into selectable tiles. The buttons and the stepper beside them paint
 * their own artwork, so each one draws the same on every platform.
 */
package com.slinky.ludus.editor.components;
