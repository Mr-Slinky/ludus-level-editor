/**
 * The strips and areas that divide the window up.
 * <p>
 * {@link com.slinky.ludus.editor.panels.TitleBar} replaces the system caption on an undecorated window,
 * {@link ControlBar} runs along the bottom, {@link MenuBar} switches the left edge from one swatch to another,
 * {@link SwatchPanel} presents the tilesets a deck at a time, and {@link MetadataView} shows the metadata of
 * the cell selected on the canvas.
 */
package com.slinky.ludus.editor.panels;
